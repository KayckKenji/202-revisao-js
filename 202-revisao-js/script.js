    let signos = ["aries", "aquario", "cancer", "peixes", "sagitario", "escorpiao", "libra", "gemeos"];
    // FARINHA
    for(let indice in signos){
        console.log("O item na posição " + indice + " é o " + signos[indice]);
    }
    console.log("---------------------------")
    // FAROFA
        for(let valores of signos){
        console.log(valores);
    }